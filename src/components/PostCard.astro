---
const { title, excerpt, image, url } = Astro.props;
---

<article class="post-card">
  <a href={url} class="post-card__link">
    {image && (
      <figure class="post-card__image">
        <img src={image} alt={title} loading="lazy" />
      </figure>
    )}

    <h2 class="post-card__title">{title}</h2>

    {excerpt && (
      <p class="post-card__excerpt">
        {excerpt}
      </p>
    )}
  </a>
</article>

<style>
/* --- Tổng thể: tạo nhịp dọc, không “card” --- */
.post-card {
  padding: 3.5rem 0;
}

/* --- Link bao toàn bài, không CTA --- */
.post-card__link {
  display: block;
  text-decoration: none;
  color: inherit;
  max-width: 680px;
}

/* --- Ảnh: giống ảnh minh hoạ bài viết, không thumbnail --- */
.post-card__image {
  margin: 0 0 1.75rem;
}

.post-card__image img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 4px;
}

/* --- Title: editorial, thoáng, không headline gắt --- */
.post-card__title {
  font-size: 1.5rem;
  line-height: 1.45;
  margin: 0 0 0.75rem;
  font-weight: 500;
}

/* --- Excerpt: rất nhẹ, chỉ để dẫn nhịp --- */
.post-card__excerpt {
  font-size: 1rem;
  line-height: 1.75;
  margin: 0;
  color: #666;
  max-width: 38rem;
}

/* --- Tách bài rất nhẹ, không viền --- */
.post-card + .post-card {
  border-top: 1px solid #f0f0f0;
}
</style>
