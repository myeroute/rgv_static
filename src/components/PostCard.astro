---
const { title, excerpt, image, url } = Astro.props;
---

<article class="post-card">
  <a href={url} class="post-card__link">
    {image && (
      <figure class="post-card__image">
        <img src={image} alt={title} loading="lazy" />
      </figure>
    )}

    <h2 class="post-card__title">{title}</h2>

    {excerpt && (
      <p class="post-card__excerpt">
        {excerpt}
      </p>
    )}
  </a>
</article>

<style>
.post-card {
  padding: 3.5rem 0;
}

/* vùng đọc chữ */
.post-card__link {
  display: block;
  text-decoration: none;
  color: inherit;
  max-width: 680px;
}

/* ẢNH KHÔNG ĐƯỢC FULL-WIDTH */
.post-card__image {
  margin: 0 0 2rem;
  max-width: 520px;      /* 🔑 QUAN TRỌNG */
}

/* ảnh nằm trong vùng đọc, không chiếm sân */
.post-card__image img {
  width: 100%;
  height: auto;
  display: block;

  opacity: 0.95;
  filter: saturate(0.95) contrast(0.97);
  border-radius: 4px;
}

/* Title dẫn nhịp */
.post-card__title {
  font-size: 1.55rem;
  line-height: 1.45;
  margin: 0 0 0.9rem;
  font-weight: 500;
}

/* Excerpt nhẹ */
.post-card__excerpt {
  font-size: 1rem;
  line-height: 1.75;
  margin: 0;
  color: #666;
  max-width: 38rem;
}

/* tách bài rất nhẹ */
.post-card + .post-card {
  border-top: 1px solid #f0f0f0;
}
</style>
